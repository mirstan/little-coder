# Pinned to the 3.x line: components.py and metric.py's ScoreWithFeedback
# import (dspy.teleprompt.gepa.gepa) target internals confirmed against
# installed dspy-ai 3.3.1 -- the previous >=2.5,<3.0 constraint let a fresh
# install resolve an incompatible 2.x whose GEPA module layout differs,
# real bug confirmed by review (would fail deep inside GEPA.compile() after
# reflection_lm budget was already spent).
dspy-ai>=3.3,<4.0
pydantic>=2
pyyaml
python-dotenv
pytest
